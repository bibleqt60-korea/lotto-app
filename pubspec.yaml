name: lotto_app

description: 로또 6/45 통계 및 번호 생성 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.4.0
  shared_preferences: ^2.5.3
  google_mobile_ads: ^6.0.0
  url_launcher: ^6.3.2
  geolocator: ^14.0.2
  flutter_local_notifications: ^19.4.0
  workmanager: ^0.9.0
  timezone: ^0.10.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true